language: python
branches:
  only:
    - master
python:
  - "2.7"
os:
  - linux
  - osx
sudo: false
install:
  - make install
script:
  - make test
#after_success:
# - coveralls
matrix:
  allow_failures:
    - os: osx
